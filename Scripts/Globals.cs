public enum TurretType
{
    MachineGun,
    RocketLauncher,
    Laser
};

public enum ProjectileType
{
    Bullet,
    Rocket,
    Laser
}

public enum ScreenType
{
    Playing,
    EndScreen,
    MainMenu,
    Controls,
    Leaderboard,
    Pause
}
